<div xmlns="http://www.w3.org/1999/xhtml" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://hl7.org/fhir ../../src-generated/schemas/fhir-single.xsd">
	<a name="Description"/>
	<h3>Description</h3>
	<p>Retrieve count or list of patients having specified genotypes/haplotypes.</p>

	<p>Population queries are designed to return a <b>count</b> of patients that match each item sought, with or without a <b>list</b> of patients matching the item(s) sought.</p>

	
		<p>As described in <a href="operations.html#and-or-logic-in-queries">general guidance</a>, FHIR Genomics Operations do not support 'AND' logic unless stated otherwise. This particular operation supports a logical 'AND' of the 'haplotypes' parameter, specified by repeating the parameter. 'AND' logic is useful when searching for other subjects sharing a specific constellation of haplotypes. However, 'AND' logic, particularly when coupled with 'OR' logic, adds complexity to search and response. For instance, a potential search might be for subjects having:</p>

	<pre>...&amp;haplotypes=a,b,c&amp;haplotypes=d,e,f&amp;haplotypes=i,j,k</pre>
	
	<p>i.e. matching patients will have haplotype 'a' or 'b' or 'c', and will have haplotype 'd' or 'e' or 'f', and will have haplotype 'i' or 'j' or 'k'. Where 'AND' logic is used, the response will include only one numerator/denominator, applied to the entire expression. </p>
	
<pre>
"parameter": [
  {
    "name": "haplotypes",
    "part": [
      {
        "name": "haplotypeItem",
        "valueString": "a|b|c AND d|e|f AND i|j|k"
      },
      {
        "name": "numerator",
        "valueQuantity": {"value": 17}
      }
  ]
</pre>

	
	
</div>

